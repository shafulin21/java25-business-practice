<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.infra.mapper.ProductMapper">
    <resultMap id="ProductMap" type="com.demo.infra.mapper.ProductMapper">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.demo.infra.mapper.ProductMapper"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product(name,price,stock,status)
        VALUES(#{name}, #{price}, #{stock, #{status})
    </insert>
    
    <select id="selectById" resultMap="ProductMap">
        SELECT * FROM product WHERE id = #{id}
    </select>

</mapper>